import React from 'react';
import { Switch, Route, BrowserRouter } from 'react-router-dom';

const App = () => (
  <BrowserRouter>
    <Switch>
      <Route></Route>
    </Switch>
  </BrowserRouter>
);

export default App;
